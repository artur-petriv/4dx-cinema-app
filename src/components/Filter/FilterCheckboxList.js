import React from 'react';
import styled from 'styled-components';

const FilterCheckboxContainer = styled.div`
  display: grid;
  row-gap: 12px;
`;

export default function FilterCheckboxList() {
  return <FilterCheckboxContainer></FilterCheckboxContainer>;
}
